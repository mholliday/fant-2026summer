# Leave blank to use Vite dev proxy (recommended for development).
# Set to the full backend URL for production builds.
# Note: Vite requires the VITE_ prefix — REACT_APP_ will NOT work.
VITE_API_URL=
